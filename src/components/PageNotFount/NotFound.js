import { Link } from 'react-router-dom';

const NotFound = () => {
  return (
    <div>
      <p>
        <Link to="/articles">Go to Home </Link>
      </p>
    </div>
  );
};

export default NotFound;
